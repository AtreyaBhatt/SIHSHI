# Node / extension build
node_modules/
dist/
build/
*.tsbuildinfo

# Python / server
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# Model weights — large binaries; document source + checksum in extension/models/README.md
extension/models/**/*.onnx
extension/models/**/*.bin
extension/models/**/*.wasm

# Local secrets & config
.env
.env.local

# Eval outputs
eval/results/
eval/**/*.local.json

# OS / editor
.DS_Store
*.swp

# esbuild-installed platform binaries
extension/node_modules/

# Test photographs of real people. Fetched on demand, never committed.
eval/fixtures/assets/

# Design canvas: assembled output, regenerate with build.py + the design skill
design/**/*.html
!design/**/*.dc.html
